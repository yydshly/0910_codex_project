# 来源与验证记录

## 版本与许可

上游为 [rawfilejson/awesome-osint-arsenal](https://github.com/rawfilejson/awesome-osint-arsenal)，固定提交 `2c6475a1d5b941cc598b3612419ef22e6d903ce8`，提交时间为 2026-08-29 03:14:12 UTC。研究日期：2026-09-10。

读取 README、tools.json、LICENSE、总安装脚本与分类脚本。保留 tools.json 原始快照与 MIT 许可证，没有将完整上游仓库放入正式项目。第三方工具和网站适用各自许可与条件。

## 数据核对

753 条记录、752 个唯一 ID、26 种 category；重复 ID 为 social-searcher。获取方式：web 300、manual 161、git 117、pip 70、apt 62、go 35、docker 8。

页面不对非唯一 ID 静默去重，保留全部记录。只接受 http/https 网址；空网址回到固定 README，不猜造地址。原文按文本显示，不能执行 HTML 或安装命令。README 的 50 类与 JSON 的 26 个分类值口径不同。

## 真实安装尝试

1. 只读检查发现现有 WSL Ubuntu 22.04，没有安装 Linux。
2. 直接运行原版 install.sh，因非 root 身份退出（退出码 1），没有进入分类模块。
3. 准备了仅安装 ExifTool 的研究脚本，但 sudo 因需要密码阻止了执行（退出码 1），没有成功安装 ExifTool。
4. 用户要求暂时不安装后停止此方向，没有继续提权、改变权限或运行调查工具。

原始退出输出保存在项目 evidence 目录；不作为安装成功或功能可用的证据。研究脚本仅保留复核用途，不由构建或网页调用。

## 展示验证

本次整理将摘要统一为“以工具查找为主，提供工具目录、获取方式与安装脚本”，目录检索移到网页前部。新增正式完整理解图及可复现绘图源，补充“只搜索已有清单、不自动发现新工具、不自动同步上游”的文字。完整图已目视检查；本项目 20 处本地引用、搜索与组合过滤、场景引用和图片字节一致性检查通过。

检查程序已通过：原始数据一致性、753/752/26 统计、获取方式计数、六个场景的工具引用、中文与英文搜索、组合过滤、空结果、网址协议约束，以及本项目 16 处本地引用。统一构建九个展示通过，414 处本地引用通过子路径检查。app.mjs 语法与 SVG XML 解析检查通过。

使用现有 Node.js 提供静态网页，没有新增应用依赖，没有后台调查服务，不收集或上传目标数据。需通过 HTTP 服务静态文件，不假设 file:// 可以加载 JSON。

可选 WebMCP 在支持的浏览器中提供目录筛选与场景切换；没有可用的真实 WebMCP 验证上下文，不能声称浏览器注册契约已验证。此项不影响普通目录浏览。

未执行真实浏览器截图、点击与视觉回归。未逐项验证外链可达性、最新价格、安装成功率或工具实际效果。页面场景均为用途说明，没有虚构的运行结果。

[完整理解](01-understanding.md)
