# 仓库协作约定

- 本仓库用于研究多个优秀 GitHub 项目，默认使用中文文档。
- 根 README 负责摘要、有序项目索引和代表性图片；详细研究内容写入对应子项目。
- 正式项目放在 `projects/<编号>-<英文名称>/`，编号从 `001` 递增、保持稳定且不复用；索引和预览按编号数值升序排列。
- 新增项目复用 `templates/project/`，遵循 `docs/adding-projects.md`，同步更新根 README。
- 图片放在子项目 `assets/`，使用相对链接，并添加替代文字、图注与来源。
- 记录上游仓库、研究版本和许可证；未实际验证的能力或结论明确标注。
- 每个 Web 演示独立存放在子项目 `web/`；部署遵循 `docs/web-demos.md`，仅在上线并验证后添加真实演示链接。
- 根据具体子项目选择技术栈，不为纯文档研究提前增加应用依赖。
- 不提交密钥、依赖目录、构建产物，或未经整理的完整上游仓库副本。
